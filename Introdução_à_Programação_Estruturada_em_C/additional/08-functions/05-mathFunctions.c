#include <math.h>

// https://www.w3schools.com/c/c_math.php
int main()
{
  // Raiz quadrada;
  printf("%f", sqrt(16));

  // arredondar um numero
  printf("%f", ceil(1.4));
  printf("%f", floor(1.4));

  // potenciação
  printf("%f", pow(4, 3));

  // consulte o link para +
  return 0;
}
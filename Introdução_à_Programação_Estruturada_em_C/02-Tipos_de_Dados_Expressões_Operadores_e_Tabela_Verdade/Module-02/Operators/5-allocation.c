#include <stdio.h>

int main(){
  // =
  float peso = 70.8;
  float altura = 1.90;
  float IMC = peso/ (altura*altura);

  printf("IMC %.2f\n", IMC);

  // resumos
  // a + = b;
  // a - = b;
  // a* = b;
  // a/ = b;
  // a % = b;
  // a & = b;
  // a| = b;
  // a^ = b
  // a <<= b;
  // a >>= b;

  return 0;
}